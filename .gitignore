__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/
dist/
build/
*.egg-info/
results/logs/
results/tmp/
results/refs/
results/eval/
results/evalB/
results/eval*/
results/variance/
results/spend.json
results/spend.json.lock
results/nightly/
site/build/
*.log
.DS_Store
Thumbs.db

# cocotb / sim transient artifacts
results.xml
**/results.xml
sim_build/
**/sim_build/

# SymbiYosys formal working directory (task name matches [options] task name in each props.sby)
**/formal/props/

# Published paper table artifacts: track only the .tex fragments the paper \inputs
# (generated, but must ship so the paper compiles from a clean checkout without results/)
!paper/data/build/
paper/data/build/*
!paper/data/build/*.tex

# stray flock test locks
results/*.lock

# LaTeX build artifacts
paper/*.pdf
paper/*.bbl
paper/*.aux
paper/*.log
paper/*.out
paper/*.blg

# compiled paper output
paper/out/

# arXiv build bundle (never commit into the public tree)
paper/*.tar.gz

# local agent tooling
.claude/skills/

# External benchmark sources are fetched from pinned revisions and never redistributed.
external-audit/vendor/
